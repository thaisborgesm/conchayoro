import { ProviderData } from '../index.js';

declare function getLaunchDarklyData(options: {
    apiKey: string;
    environment: string;
    projectKey: string;
}): Promise<ProviderData>;

export { getLaunchDarklyData };
