import { ProviderData } from '../index.js';

declare function getStatsigData(options: {
    consoleApiKey: string;
    /**
     * Required to set the `origin` property on the flag definitions.
     */
    projectId?: string;
}): Promise<ProviderData>;

export { getStatsigData };
